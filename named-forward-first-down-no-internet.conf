options {
        listen-on port 53 { any; };
        listen-on-v6 port 53 { none; };
        directory       "/var/named";
        allow-query     { any; };
        recursion yes;

        dnssec-validation no;

        forward first;
        forwarders { 192.168.66.25; 192.168.66.26; };

        pid-file "/run/named/named.pid";
};

zone "." IN {
        type hint;
        file "/etc/named.ca";
};
